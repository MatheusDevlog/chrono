# ─── Python ───────────────────────────────────────
# Ambiente virtual (pesado e específico do seu PC — nunca vai pro repo)
.venv/
__pycache__/
*.pyc

# ─── Segredos / ambiente ──────────────────────────
.env
*.local

# ─── Windows ──────────────────────────────────────
Thumbs.db
Desktop.ini

# ─── Editores ─────────────────────────────────────
.vscode/
.idea/

# ─── Material pessoal de estudo (não publicar) ────
GUIA/

# ─── Dados locais do app (histórico) ──────────────
*.db
app/dados/

# ─── Empacotamento (PyInstaller / Inno Setup) ─────
build/
dist/
Output/
*.spec
# ...menos o Chrono.spec, que é config de build (versionada de propósito)
!app/Chrono.spec